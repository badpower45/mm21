# دليل نشر نظام POS

## الوضع الحالي: التخزين المحلي ✅

النظام الآن يعمل بالكامل في **الوضع المحلي** باستخدام Local Storage في المتصفح. هذا يعني:

- ✅ لا حاجة لإعدادات إضافية
- ✅ البيانات محفوظة في المتصفح
- ✅ يعمل بدون إنترنت
- ⚠️ البيانات خاصة بكل متصفح (لا تُشارك بين الأجهزة)

## الترقية إلى Supabase (اختياري) 🚀

إذا كنت تريد مشاركة البيانات بين أجهزة متعددة، يمكنك نشر Edge Function على Supabase:

### 1. تثبيت Supabase CLI

```bash
# macOS/Linux
brew install supabase/tap/supabase

# Windows
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase
```

### 2. تسجيل الدخول

```bash
supabase login
```

### 3. ربط المشروع

```bash
supabase link --project-ref gdebagedycaqulrxvjrw
```

### 4. نشر Edge Function

```bash
supabase functions deploy server
```

### 5. تفعيل KV Store

في لوحة تحكم Supabase:
1. انتقل إلى Edge Functions
2. افتح إعدادات الدالة `server`
3. فعّل Deno KV

## حالة النظام الحالية

🟢 **النظام يعمل بنجاح** - يستخدم التخزين المحلي تلقائياً

- إذا كان Supabase متاحاً → يستخدمه
- إذا كان Supabase غير متاح → يستخدم Local Storage

## ميزات كل وضع

### الوضع المحلي (Local Storage)
- ✅ سرعة عالية
- ✅ لا يحتاج اتصال إنترنت
- ✅ سهل الاستخدام
- ❌ البيانات لا تُشارك بين الأجهزة

### وضع Supabase
- ✅ مشاركة البيانات بين الأجهزة
- ✅ نسخ احتياطي تلقائي
- ✅ مزامنة فورية
- ❌ يحتاج اتصال إنترنت

## استخدام النظام

### تسجيل الدخول

**المالك:**
- اسم المستخدم: `owner`
- كلمة المرور: `123456`

**الموظف:**
- اسم المستخدم: `cashier`
- كلمة المرور: `123456`

## حل المشاكل

### الرسائل التحذيرية عادية

إذا رأيت:
```
⚠️ فشل الاتصال بـ Supabase - سيتم استخدام التخزين المحلي
💾 النظام يعمل في الوضع المحلي
```

هذا **طبيعي وليس خطأ** - النظام يعمل بشكل صحيح في الوضع المحلي.

### لا تظهر البيانات

تأكد من:
1. استخدام نفس المتصفح
2. عدم مسح بيانات المتصفح
3. تسجيل الدخول بنجاح

## الدعم

للمزيد من المساعدة، راجع:
- `SYSTEM_GUIDE.md` - دليل استخدام النظام
- `EMPLOYEE_GUIDE.md` - دليل الموظفين
